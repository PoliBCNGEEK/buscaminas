{
  "scripts": {
    "test:e2e": "cucumber-js --require cucumber.conf.js --require tests/steps/**/*.steps.js --format @cucumber/pretty-formatter --format html:test/report/cucumber-report.html --parallel 4"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^8.6.0",
    "playwright": "^1.26.1"
  },
  "dependencies": {
    "@cucumber/pretty-formatter": "^1.0.0"
  }
}
